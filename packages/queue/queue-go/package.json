{
  "name": "@alloy/queue-go",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "sh -c 'mkdir -p ../../../infra/.cache/turbo/dist/queue && go build ./... && touch ../../../infra/.cache/turbo/dist/queue/.build-stamp'",
    "dev": "go test -race ./... -count=1",
    "test": "go test -race ./...",
    "test:coverage": "sh -c 'mkdir -p ../../../infra/.cache/coverage/go/queue && go test -race ./... -coverprofile=../../../infra/.cache/coverage/go/queue/coverage.out'",
    "typecheck": "go test ./... -run '^$'",
    "fmt": "echo gofmt handled by root go-fmt sweep",
    "fmt:check": "sh -c 'test -z \"$(gofmt -l .)\"'"
  }
}
