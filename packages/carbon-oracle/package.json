{
  "name": "@tempo/carbon-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "oracle:check": "tsx src/generate.ts --check",
    "oracle:generate": "tsx src/generate.ts",
    "test": "tsx src/generate.ts --check",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@tempo/artefacts": "workspace:*",
    "@types/node": "^25.0.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
