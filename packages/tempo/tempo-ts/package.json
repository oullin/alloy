{
  "name": "@alloy/tempo",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "imports": {
    "#*": "./src/*"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsdown",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "pnpm build",
    "test": "pnpm --filter @alloy/tempo-acceptance test:tempo-ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsdown": "^0.15.6",
    "typescript": "^6.0.3"
  }
}
