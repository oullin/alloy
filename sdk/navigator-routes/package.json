{
  "name": "@alloy/sdk/navigator-routes",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE.md",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oullin/alloy.git",
    "directory": "sdk/navigator-routes"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "rm -rf dist ../../infra/.cache/tsbuild/navigator-routes-build.tsbuildinfo && tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepack": "pnpm build",
    "test": "vp test run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
